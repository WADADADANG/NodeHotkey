# คู่มือสถาปัตยกรรม Node Registry (NodeHotkey v3.1)
> **เอกสารสรุปความคืบหน้า สถาปัตยกรรม และแผนการย้ายโหนด (กันลืม / สำหรับพัฒนาต่อ)**

---

## 📌 1. สถานะโปรเจกต์ปัจจุบัน (Project Milestones)

| เวอร์ชัน | สถานะ | กิ่ง Git (Branch) | รายละเอียด |
| :--- | :--- | :--- | :--- |
| **v3.0.0** | ✅ **สมบูรณ์ / เผยแพร่แล้ว** | `main` (Tag: `v3.0.0`) | - แก้บั๊ก Party Scanner ลูปบัฟซ้ำเสร็จสมบูรณ์<br>- เพิ่มโหนด Screenshot วิเคราะห์ปัญหา Zero-Flicker<br>- เผยแพร่บน GitHub Releases พร้อมไฟล์ติดตั้ง `.exe` (141 MB) |
| **v3.1.0-dev** | 🚀 **กำลังพัฒนา** | `v3.1-node-registry` | - วางรากฐานสถาปัตยกรรม **Modular Node Registry** (1 โหนด = 1 ไฟล์)<br>- ทดสอบย้ายโหนดต้นแบบ 2 โหนดแรกสำเร็จ (`screenshot`, `tts`)<br>- เชื่อมต่อระบบคู่ขนานใน `bot.js` ปลอดภัย 100% ไม่กระทบโหนดเดิม |

---

## 💡 2. ทำไมต้องเปลี่ยนเป็นระบบ Node Registry?

### ❌ ปัญหาเดิม (Switch-Case Hell):
เดิมเวลาจะเพิ่มหรือแก้ไข Action Node เพียง 1 ตัว ต้องตามไปไล่แก้โค้ดแทรก `if-else` กระจายกันอยู่ถึง **6 ไฟล์**:
1. `public/js/canvas.js` (ลงทะเบียน Type, วาดพอร์ต, เขียน HTML Inspector)
2. `public/js/components/actions.js` (เช็คโหมดของแอ็กชัน)
3. `public/js/i18n.js` (แปลภาษา)
4. `execution-engine.js` (จับคู่โหนดใน Graph Engine)
5. `converter.js` (แปลง Type ระหว่าง Profile กับ Node)
6. `bot.js` (เช็ค `else if (act.mode === '...')` ซ้ำกัน 2 ลูปเพื่อสั่งรัน)

### ✅ สถาปัตยกรรมใหม่ v3.1 (1 โหนด = 1 ไฟล์ จบในตัว):
- ย้ายโค้ดของแต่ละโหนดมาไว้ในโฟลเดอร์ `nodes/*.node.js`
- ภายในไฟล์เดียวจะประกอบด้วย:
  - **Metadata**: ชื่อ, ไอคอน, สี, หมวดหมู่
  - **Ports**: รายการพอร์ตฝั่งเข้า (`inputs`) และฝั่งออก (`outputs`)
  - **Default Data**: ค่าเริ่มต้นของโหนด
  - **Execute Handler**: ฟังก์ชัน `execute(context, action, callStack)` สำหรับรันคำสั่งจริง
- ไฟล์แกนกลางอย่าง `bot.js` และ `canvas.js` จะเหลือเพียงคำสั่งเรียก Registry สั้นๆ:
  ```javascript
  if (global.nodeRegistry && global.nodeRegistry.has(action.mode)) {
      await global.nodeRegistry.execute(action.mode, context, action, callStack);
      return;
  }
  ```

---

## 🛠️ 3. สิ่งที่สร้างเสร็จแล้วในระบบ (Implemented Core)

1. **`node-registry.js`**: แกนกลางฝั่ง Backend ทำหน้าที่ค้นหาและโหลดทุกไฟล์ `*.node.js` ในโฟลเดอร์ `nodes/` อัตโนมัติเมื่อเริ่มระบบ รองรับชื่อ Alias สำรอง
2. **`public/js/node-registry-client.js`**: ตัวจัดการฝั่ง Frontend สำหรับ Canvas
3. **`test-server.js`**: มี Endpoint `GET /api/nodes` ส่งรายชื่อโหนดโมดูลทั้งหมดให้หน้าเว็บ
4. **`public/js/canvas.js`**: ฉีดโหนดจาก Registry เข้าเมนู Spotlight คลิกขวาหมวด **"🧩 โหนดโมดูลเสริม (v3.1)"** ให้อัตโนมัติ
5. **`bot.js`**: ติดตั้ง Registry Dispatcher ทั้งในลูป Trigger เดี่ยว และลูป Chained Action ในระบบกราฟ พร้อม Fallback ไปหาระบบเดิมหากยังไม่ได้ย้าย

---

## 📋 4. แผนการย้ายโหนดทั้งหมด (Migration Checklist)

| กลุ่มงาน | โหนด (Node Type) | สถานะ | หน้าที่การทำงาน | ไฟล์ปลายทาง |
| :--- | :--- | :---: | :--- | :--- |
| **Vision & Party** | `screenshot` | ✅ ย้ายแล้ว | ถ่ายภาพหน้าจอ Native Compositor วิเคราะห์ปัญหา | `nodes/screenshot.node.js` |
| | `party_scanner` | ⏳ รอคิว | สแกนหลอดเลือดและรายชื่อสมาชิกปาร์ตี้ | `nodes/party_scanner.node.js` |
| | `party_buff` | ⏳ รอคิว | วนเลือกสมาชิกแจกบัฟตามลำดับ | `nodes/party_buff.node.js` |
| | `party_heal` | ⏳ รอคิว | ตรวจจับเลือดลดและฮีลสมาชิกอัตโนมัติ | `nodes/party_heal.node.js` |
| | `party_slot` | ⏳ รอคิว | คลิกเลือกสล็อตเป้าหมายเจาะจง (เช่น Slot 1) | `nodes/party_slot.node.js` |
| | `party_target` | ⏳ รอคิว | Router กระจายเป้าหมาย (Heal Priority / Buff Loop) | `nodes/party_target.node.js` |
| **Keyboard & Automation** | `key_press` | ⏳ รอคิว | กดปุ่มเดี่ยวตามจอเป้าหมาย | `nodes/key_press.node.js` |
| | `key_hold` | ⏳ รอคิว | กดปุ่มค้าง / สลับเปิด-ปิด (Hold Toggle) | `nodes/key_hold.node.js` |
| | `loop` | ⏳ รอคิว | ลูปกดปุ่มต่อเนื่องตามความถี่ที่ตั้งไว้ | `nodes/loop.node.js` |
| | `buff_sequence` | ⏳ รอคิว | รันชุดสกิลบัฟตามระยะเวลาคูลดาวน์ | `nodes/buff_sequence.node.js` |
| | `sequencer` | ⏳ รอคิว | Cast Sequencer กดสกิลเป็นสเต็ป | `nodes/sequencer.node.js` |
| | `loop_scheduler` | ⏳ รอคิว | จัดตารางวนลูปพร้อมสุ่มหน่วงเวลา (Jitter) | `nodes/loop_scheduler.node.js` |
| | `macro_group` | ⏳ รอคิว | คอมโบปุ่มแบบกำหนดสเต็ปและเวลาละเอียด | `nodes/macro_group.node.js` |
| **Logic & Flow** | `delay` | ⏳ รอคิว | หน่วงเวลาก่อนส่งสัญญาณไปโหนดถัดไป | `nodes/delay.node.js` |
| | `branch` (condition) | ⏳ รอคิว | แยกสายการทำงานตามเงื่อนไข (If / Else) | `nodes/branch.node.js` |
| | `variable` | ⏳ รอคิว | บันทึกและสลับค่าตัวแปร (Boolean, Number) | `nodes/variable.node.js` |
| | `control` | ⏳ รอคิว | สั่งเปิด / ปิด / หยุดโหนดอื่นในกราฟ | `nodes/control.node.js` |
| | `forwarder` | ⏳ รอคิว | ส่งสัญญาณกระจายไปยังจอ Client อื่น | `nodes/forwarder.node.js` |
| | `emit_event` | ⏳ รอคิว | ยิงสัญญาณ Custom Event ข้ามระบบ | `nodes/emit_event.node.js` |
| **Utility & Alerts** | `tts` | ✅ ย้ายแล้ว | สังเคราะห์และเล่นเสียงพูดเตือน (Text-to-Speech) | `nodes/tts.node.js` |
| | `sound` | ⏳ รอคิว | เล่นไฟล์เสียงเตือน (.mp3, .wav) | `nodes/sound.node.js` |
| | `emergency_stop` | ⏳ รอคิว | ปุ่มหยุดฉุกเฉิน ยุติการทำงานทุกอย่าง | `nodes/emergency_stop.node.js` |
| | `webhook_out` | ⏳ รอคิว | ส่งข้อมูลแจ้งเตือนเข้า Discord / HTTP Webhook | `nodes/webhook_out.node.js` |

---

## 🧩 5. วิธีสร้าง Action Node ใหม่ใน 3 นาที (Node Template)

เมื่อต้องการสร้างโหนดใหม่ ให้สร้างไฟล์ชื่อ `nodes/<ชื่อโหนด>.node.js` ตามรูปแบบนี้ได้ทันที:

```javascript
/**
 * nodes/my_custom.node.js
 * Template ตัวอย่างสำหรับสร้าง Action Node ใหม่
 */

module.exports = {
  type: 'my_custom',                         // รหัสประจำโหนด (ห้ามซ้ำ)
  aliases: ['custom_alias'],                 // ชื่อเรียกสำรอง (ถ้ามี)
  title: 'My Custom Action',                 // ชื่อแสดงผลบน Canvas
  category: 'Utility',                       // หมวดหมู่
  icon: '⚡',                                 // ไอคอน
  color: '#3b82f6',                          // สีธีมประจำโหนด
  inputs: ['in'],                            // พอร์ตฝั่งซ้าย
  outputs: ['onComplete', 'onError'],        // พอร์ตฝั่งขวา
  defaultData: {
    targetClient: '1',
    customSetting: 'hello'
  },

  /**
   * ฟังก์ชันที่จะถูกเรียกเมื่อบอททำงานมาถึงโหนดนี้
   * @param {Object} context ข้อมูลระบบ เช่น { clientPages, activeClients }
   * @param {Object} action ข้อมูลค่าตัวแปรที่ตั้งไว้ในโหนด
   * @param {Array} callStack สำหรับส่งต่อเพื่อป้องกันลูปวนไม่รู้จบ
   */
  async execute(context, action, callStack = []) {
    try {
      console.log(`⚡ [MyCustomNode] Executing on Client ${action.targetClient}...`);

      // เขียนคำสั่งการทำงานจริงที่นี่
      // ...

      // ส่งสัญญาณไปพอร์ตถัดไปเมื่อสำเร็จ
      if (typeof global.fireChain === 'function') {
        await global.fireChain(action, 'onComplete', callStack);
      }
      return true;
    } catch (err) {
      console.error(`❌ [MyCustomNode] Error:`, err.message);
      // ส่งสัญญาณไปพอร์ต onError หากเกิดข้อผิดพลาด
      if (typeof global.fireChain === 'function') {
        await global.fireChain(action, 'onError', callStack);
      }
      return false;
    }
  }
};
```

---

## 🔒 6. หลักความปลอดภัยและข้อควรจำ
1. **Zero-Breaking Principle**: โหนดใหม่หรือโหนดที่ย้าย จะต้องไม่ทำให้โปรไฟล์เก่าใน `configs/profiles/` พัง
2. **Backward Compatibility**: `bot.js` จะยังคงรักษา Fallback ไปหาโค้ดเดิมไว้เสมอจนกว่าทุกโหนดจะถูกย้ายและทดสอบจนเสถียร 100%
3. **Commit บ่อยๆ**: เมื่อย้ายโหนดสำเร็จทีละกลุ่ม ให้ทำการ `git commit` และ `git push` ขึ้น Branch `v3.1-node-registry` เสมอ
